import { MerediemConverterPipe } from './merediem-converter.pipe';

describe('MerediemConverterPipe', () => {
  it('create an instance', () => {
    const pipe = new MerediemConverterPipe();
    expect(pipe).toBeTruthy();
  });
});
