import { EnumOptionsPipe } from './enum-options.pipe';

describe('EnumOptionsPipe', () => {
  it('create an instance', () => {
    const pipe = new EnumOptionsPipe();
    expect(pipe).toBeTruthy();
  });
});
