import { SetNullDirective } from './set-null.directive';

describe('SetNullDirective', () => {
  it('should create an instance', () => {
    const directive = new SetNullDirective();
    expect(directive).toBeTruthy();
  });
});
