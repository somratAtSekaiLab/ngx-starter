import { IeTypeaheadDirective } from './ie-typeahead.directive';

describe('IeTypeaheadDirective', () => {
  it('should create an instance', () => {
    const directive = new IeTypeaheadDirective();
    expect(directive).toBeTruthy();
  });
});
